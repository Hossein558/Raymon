// Unable to add package declaration. The parent folder names contain identifiers that are not valid in a package name.

import com.onresolve.scriptrunner.db.DatabaseUtil

// def issue = Issues.getByKey('RAYMON-4')
/*
def status = issue.status.name.toString()
def creator = issue.creator.username.toString()


log.warn(status)

if (status == 'اصلاح درخواست') {
  issue.update {
    setCustomFieldValue('Editors', issue.reporter)
  }
} else {
  issue.update {
    setCustomFieldValue('Editors', '')
  }
}
*/